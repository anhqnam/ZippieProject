<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical" >

        <unique.packagename.widget.TitleBar
        android:id="@+id/title_layout"
        android:background="@color/row_primary"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/bars_height"
        app:label="@string/help_feedback"/>

        <RelativeLayout
            android:id="@+id/layout_help_text"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center" >

            <ImageView
                android:id="@+id/feedback_bg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:src="@drawable/messages_list_msg" />

            <unique.packagename.widget.MainEditText
                android:id="@+id/feedback_text"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:layout_centerInParent="true"
                android:background="@color/transparent"
                android:gravity="center"
                android:padding="10dp"
                android:hint="@string/feedback_hint"
                android:textColor="@color/text_editable"
                android:textColorHint="@color/text_empty_msg" />
        </RelativeLayout>

        <unique.packagename.widget.MainTextView
            android:id="@+id/feedback_caracters_left"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="right"
            android:textAppearance="@android:style/TextAppearance.Medium"
            android:textColor="@color/row_primary"
            android:visibility="gone" />
    </LinearLayout>

    <include
        android:id="@+id/feedback_send"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/registration_btn_size"
        android:layout_alignParentBottom="true"
        layout="@layout/feedback_button_send" />

</LinearLayout>